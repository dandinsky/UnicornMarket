<%= form_with(model: content) do |form| %>
  <% if content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(content.errors.count, "errori") %> hanno impedito all'articolo di essere aggiunto:</h2>

      <ul>
        <% content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titolo, style: "display: block" %>
    <%= form.text_field :titolo %>
  </div>

  <div>
    <%= form.label :descrizione, style: "display: block" %>
    <%= form.text_area :descrizione %>
  </div>

  <div>
    <%= form.label :prezzo, style: "display: block" %>
    <%= form.text_field :prezzo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
